# คู่มือการใช้งานระบบติดตามและประเมินโครงการวางท่อขยายเขตจำหน่ายน้ำประปา กปภ.ข.6
**สำหรับผู้เริ่มต้นใช้งาน (Beginner User Manual)**

ระบบนี้คือแดชบอร์ดติดตามข้อมูลโครงการขยายเขตและวางท่อจ่ายน้ำประปาของ การประปาส่วนภูมิภาคเขต 6 (กปภ.ข.6) ครอบคลุมพื้นที่ 8 สาขายุทธศาสตร์ในภาคตะวันออกเฉียงเหนือตอนบน โดยทำหน้าที่วิเคราะห์เชิงพื้นที่ (Geographic Mapping) ติดตามผลสัมฤทธิ์รายเดือน และประเมินจุดคุ้มทุน (Break-even Analysis) ตลอดจนการใช้น้ำจริงสะสมของผู้ใช้บริการโดยเชื่อมโยงข้อมูลโดยตรงจากฐานข้อมูลระบบ PCIS

---

## 1. การแบ่งแยกสิทธิ์การเข้าใช้งานระบบ (Role-Based Access Control)

ระบบมีการจำแนกสิทธิ์ผู้ใช้งาน (Roles) เพื่อควบคุมขอบเขตความปลอดภัยและการดำเนินงานออกเป็น **3 ระดับ** โดยจำแนกรายละเอียดและสิทธิ์การทำงาน (Permissions Matrix) ดังนี้:

| ฟังก์ชันการทำงาน / สิทธิ์ในระบบ | ผู้ดูแลระบบ <br>(Admin) | เจ้าหน้าที่แผนงาน <br>(Planning) | ผู้ใช้งานทั่วไป <br>(User / Viewer) |
| :--- | :---: | :---: | :---: |
| **ระดับความสำคัญของสิทธิ์ (Level)** | **100** | **50 - 99** | **0 - 49** |
| **1. ดูภาพรวมระบบ & แดชบอร์ด (Read Dashboard)** |  |  |  |
| - เข้าถึงหน้าจอวิเคราะห์โครงการ / แผนที่ภาพรวม | ✓ | ✓ | ✓ |
| - ดูสถิติรายเดือน / ตารางเปรียบเทียบผลสัมฤทธิ์ | ✓ | ✓ | ✓ |
| - ดูรายงานประเมินความคุ้มทุนและปริมาณใช้น้ำสะสม | ✓ | ✓ | ✓ |
| - ส่งออกข้อมูลรายชื่อลูกค้าเป็น CSV | ✓ | ✓ | ✓ |
| **2. จัดการข้อมูลโครงการ (Project Management)** |  |  |  |
| - เพิ่มโครงการใหม่เข้าระบบ (Add Project) | ✓ | ✓ | ✗ |
| - แก้ไข/อัปเดตเลขที่สัญญาและวันที่เสร็จสิ้นโครงการ | ✓ | ✓ | ✗ |
| - คำนวณพิกัดโครงการและผลการดำเนินงานใหม่ | ✓ | ✓ | ✗ |
| **3. ควบคุมและดูแลระบบ (System Administration)** |  |  |  |
| - เข้าถึงแท็บแผงควบคุมแอดมิน (Admin Console) | ✓ | ✗ | ✗ |
| - จัดการผู้ใช้งาน (เปิดใช้งาน/ระงับบัญชี) | ✓ | ✗ | ✗ |
| - จัดระดับสิทธิ์และกำหนด Role ให้แก่ผู้ใช้ | ✓ | ✗ | ✗ |
| - ตรวจสอบประวัติกิจกรรมระบบ (System Audit Logs) | ✓ | ✗ | ✗ |

> [!IMPORTANT]
> - **สิทธิ์ Admin** มีความปลอดภัยระดับสูง ไม่สามารถสั่งระงับบัญชีของตนเอง หรือลดสิทธิ์ Admin ของตนเองได้ผ่านระบบหน้าจอทั่วไป เพื่อป้องกันการล็อกระบบ (System Lockout)
> - **สิทธิ์ Planning** ถูกออกแบบมาสำหรับเจ้าหน้าที่ที่รับผิดชอบด้านการบันทึกสัญญา เพื่อกรอกเลขสัญญาที่เพิ่งได้รับและแก้ไขวันเสร็จสิ้นสำหรับคำนวณวันประเมินผลโครงการ
> - **สิทธิ์ User / Viewer** เป็นสิทธิ์เริ่มต้นของพนักงานทุกคนที่ผ่านการลงทะเบียนในระบบแบบอ่านข้อมูลอย่างเดียว (Read-only)

---

## 2. วิธีการเริ่มต้นใช้งานและเข้าสู่ระบบ (Getting Started)

### 2.1 การเปิดใช้งานระบบ (System Launching)
สำหรับผู้ดูแลระบบหรือผู้ใช้ที่ต้องการเปิดใช้งานระบบบนเครื่องคอมพิวเตอร์โลคอล ให้รันไฟล์สคริปต์ทางลัดที่อยู่ในโฟลเดอร์หลัก:
* ดับเบิลคลิกไฟล์ **`start_project.bat`**
* ระบบจะทำการสตาร์ท **MySQL Database** (Port 3306), **Backend API Server** (Port 5000) และ **Frontend Dev Server** (Port 5173) ให้โดยอัตโนมัติพร้อมเปิดเว็บเบราว์เซอร์เข้าสู่ระบบทันทีที่หน้าลิงก์: [http://localhost:5173](http://localhost:5173)

### 2.2 การลงชื่อเข้าใช้งาน (Authentication)
ระบบรองรับการเข้าสู่ระบบแบบบูรณาการ 2 ช่องทาง:
1. **ช่องทาง PWA Intranet Account (AD):** พนักงาน กปภ. สามารถป้อนชื่อผู้ใช้และรหัสผ่านตัวเดียวกับระบบสารสนเทศภายในของ กปภ. ได้ทันที โดยระบบจะทำการเชื่อมต่อไปตรวจสอบที่บริการตรวจสอบสิทธิ์กลางของ กปภ. และหากเข้าใช้งานครั้งแรก ระบบจะทำการสร้างบัญชีให้ในฐานข้อมูลแบบอัตโนมัติด้วยสิทธิ์เริ่มต้นคือ `User`
2. **ช่องทาง Local Account:** บัญชีสำหรับการตรวจสอบภายในเครื่องเซิร์ฟเวอร์หรือทดสอบระบบ (เช่น ผู้ดูแลระบบเริ่มต้น `dev` หรือบัญชี Local ที่แอดมินสร้างขึ้นเป็นพิเศษ)

---

## 3. คู่มือการใช้งานฟังก์ชันหลักรายแท็บ (Detailed Feature Walkthrough)

### 3.1 แท็บที่ 1: รายโครงการ (Projects Overview & Interactive Map)
แท็บนี้เป็นหน้าจอหลักสำหรับดูผลลัพธ์การขยายเขตจำหน่ายน้ำและวิเคราะห์ตำแหน่งทางภูมิศาสตร์

* **การ์ดสรุปผลเชิงตัวเลข (KPI Cards):** แสดงสถิติดึงข้อมูลรวมตามเงื่อนไขตัวกรอง ได้แก่ จำนวนโครงการทั้งหมด, งบประมาณที่ลงไป, เป้าหมายผู้ใช้น้ำสะสม (Target) และจำนวนผู้ใช้จริงที่ติดตั้งมาตรใช้น้ำจริง (Actual) พร้อมเปอร์เซ็นต์บรรลุเป้าหมายรวม
* **แผนที่พิกัดโครงการ (Interactive Map):** 
  - ใช้แผนที่ Leaflet แสดงผลสัญญลักษณ์หมุด (Pins) โดยมีจุดกึ่งกลางคำนวณจากค่าเฉลี่ยพิกัดของผู้ใช้น้ำจริงในโครงการนั้นๆ
  - เมื่อคลิกหมุดบนแผนที่ จะแสดงข้อมูลย่อของโครงการ
  - ด้านขวาของแผนที่จะแสดงรายละเอียดโครงการและรายชื่อผู้ใช้น้ำพร้อมพิกัดรายบุคคลบนแผนที่ย่อย (แยกสีหมุดผู้ใช้น้ำเป็นสีฟ้าสดใส)
* **กราฟเปรียบเทียบ (Analytical Charts):** กราฟแท่ง (Bar Charts) ด้านล่างแผนที่วิเคราะห์จำนวนผู้ใช้น้ำเป้าหมายเทียบกับผู้ใช้จริงแยกตาม **กปภ.สาขา** และ **ประเภทงบประมาณ**
* **ตารางรายละเอียดโครงการ (Projects Data Table):**
  - แสดงรายการแบบสืบค้น ละเอียด คัดกรองตาม **ปีงบประมาณ**, **กปภ.สาขา**, **ประเภทโครงการ** หรือค้นหาด้วยคีย์เวิร์ด
  - **สำหรับสิทธิ์ Planning และ Admin:** สามารถคลิกที่ "เลขที่สัญญา" หรือ "วันที่เสร็จสิ้นโครงการ" เพื่ออัปเดตเลขที่สัญญาและวันแล้วเสร็จจริง เพื่อให้ระบบเชื่อมโยงพิกัดผู้ใช้น้ำจากระบบ PCIS และคำนวณช่วงเวลาผลงานของโครงการใหม่
  - สามารถคลิกที่ปุ่ม **"รายชื่อผู้ใช้น้ำ"** ในแต่ละแถว เพื่อดูโปรไฟล์รายละเอียดที่อยู่อย่างละเอียดของลูกค้าแต่ละราย รวมถึงส่งออกข้อมูลเป็นไฟล์ **CSV**

---

### 3.2 แท็บที่ 2: รายเดือนรายสาขา (Monthly Performance Matrix)
หน้าจอแสดงรายงานข้อมูลผลงานการติดตั้งใช้น้ำจริงของผู้ใช้น้ำจำแนกแยกตามรายเดือน (มกราคม - ธันวาคม ตามลำดับปีงบประมาณของไทย เริ่มตั้งแต่วันที่ 1 ตุลาคมของปีก่อนหน้า จนถึง 30 กันยายนของปีถัดไป)
* **ตารางเมทริกซ์ (Matrix Grid):** แสดงตัวเลขผู้ใช้น้ำจริงที่เพิ่มในสาขาแต่ละแห่งแยกรายเดือน เพื่อให้เห็นช่วงเวลาที่ยอดติดตั้งเติบโตสูงสุด
* **การเจาะลึกข้อมูล (Drill-down):** ผู้ใช้สามารถคลิกที่ตัวเลขจำนวนผู้ใช้น้ำในแต่ละเซลล์ของสาขา/เดือนนั้นๆ เพื่อเปิดป๊อปอัพดูรายชื่อผู้ใช้น้ำทุกคนที่ติดตั้งจริงในเดือนดังกล่าว พร้อมเลขมาตรและพิกัดที่ตั้ง

---

### 3.3 แท็บที่ 3: ประเมินจำนวนผู้ใช้น้ำตามเป้าหมายโครงการ (Break-even Analysis)
ระบบการวิเคราะห์และประเมินผลการบรรลุเป้าหมายจุดคุ้มทุนโครงการ เพื่อวิเคราะห์ว่าโครงการที่ลงงบประมาณไปสามารถดึงลูกค้ามาติดตั้งได้ตามเกณฑ์หรือไม่

* **เกณฑ์การวิเคราะห์ (Payback Target Rules):**
  - **โครงการประเภท 1, 2, 3** (งบรายได้, งบอุดหนุน, งบกระตุ้นเศรษฐกิจ): ประเมินผลแบบสะสมระยะยาว **5 ปี** (ปีที่แล้วเสร็จ = เป้าหมาย 40%, ปีที่ 1-5 สะสมปีละ 15%)
  - **โครงการประเภท 4** (โครงการวางท่อเข้าซอย): ประเมินผลระยะสั้น **1 ปี** (เป้าหมาย 100% ในปีที่แล้วเสร็จ)
* **การใช้งานเครื่องมือ:**
  - เลือกโครงการที่ต้องการประเมินในแถบค้นหาขวาบน
  - กราฟเส้นแบบพิกัด (Line Charts) จะแสดงแนวโน้ม **เป้าหมายสะสม (Cumulative Target)** เทียบกับ **ผลสัมฤทธิ์จริงสะสม (Cumulative Actual)** รายปี
  - **ตารางไทม์ไลน์ (Timeline Table):** แสดงรายละเอียดการบรรลุผลในแต่ละปีการประเมิน โดยแถวที่เป็นสีเขียวคือปีที่โครงการคุ้มทุน (ยอดผลงานสะสมจริงสูงกว่าเป้าหมายสะสม) เพื่อให้ผู้บริหารมองเห็นได้ทันทีว่าโครงการคุ้มทุนตามเวลาที่กำหนดหรือไม่

---

### 3.4 แท็บที่ 4: ประเมินการใช้น้ำสะสม (Water Consumption Analytics)
เครื่องมือวิเคราะห์เชิงประสิทธิภาพที่เจาะลึกพฤติกรรมการใช้น้ำของลูกค้าในโครงการเพื่อประเมินความแอคทีฟ (Active Users) และรายได้จากการใช้น้ำ
* แสดงยอดใช้น้ำรวมรายปีงบประมาณและยอดใช้น้ำเฉลี่ยต่อคนต่อเดือน
* **ตารางข้อมูลลูกค้าและการใช้น้ำสะสม (Customer Consumption Table):**
  - แสดงรายชื่อลูกค้า รหัสผู้ใช้น้ำ ประเภทกลุ่มใช้น้ำ เลขมาตรวัดน้ำ
  - **SUM_AMT & SUM_USG:** ปริมาณน้ำสะสมที่ใช้งานจริงทั้งหมด (หน่วยเป็น ลบ.ม.) และยอดเงินรวมค่าน้ำสะสม (บาท)
  - **ตารางวิเคราะห์ย้อนหลัง 12 เดือน (M0 ถึง M12):** แสดงค่าน้ำและยอดการใช้น้ำจริงย้อนหลังเป็นรายเดือนของลูกค้าแต่ละราย เพื่อคอยตรวจจับลูกค้าที่มีการปิดมิเตอร์ทิ้งไว้ หรือใช้น้ำไม่ปกติ

---

### 3.5 แท็บที่ 5: การจัดการสิทธิ์ (Admin Management)
*สำหรับผู้ดูแลระบบ (Admin) เท่านั้น* แผงควบคุมส่วนกลางในการจัดการโครงสร้างพื้นฐานของระบบ ประกอบด้วย 3 เมนูย่อย:

1. **รายชื่อผู้ใช้งาน (User Accounts):**
   - แสดงบัญชีผู้ใช้งานที่เข้าระบบทั้งหมด แยกประเภทบัญชี (Local หรือ PWA Intranet)
   - แอดมินสามารถกำหนดสิทธิ์ (Role) ผ่านแถบดรอปดาวน์ได้ทันที และระบบจะบันทึกการกระทำลงในประวัติ
   - สามารถกดปุ่ม **"ระงับบัญชี"** หรือ **"เปิดใช้งาน"** บัญชีผู้ใช้เพื่อความปลอดภัย
2. **จัดการระดับสิทธิ์ (Role Management):**
   - สามารถกดปุ่ม **"สร้างระดับสิทธิ์ใหม่"** กำหนดระดับสิทธิ์ Level (0-100) และคำอธิบายหน้าที่
   - แอดมินสามารถแก้ไขหรือลบสิทธิ์ที่สร้างขึ้นเองได้ (แต่ระบบจะล็อคไม่ให้ลบสิทธิ์หลักอย่าง `admin` และ `user` เพื่อความมั่นคงของสถาปัตยกรรมระบบ)
3. **ประวัติการใช้งานระบบ (System Audit Logs):**
   - แสดงรายการบันทึก (Audit logs) กิจกรรมของผู้ใช้ทุกคนอย่างเป็นระบบเรียงตามเวลาล่าสุด เช่น วันเวลาเข้าสู่ระบบ (LOGIN), การอัปเดตสิทธิ์ (UPDATE_ROLE), การอัปเดตเลขที่สัญญา (UPDATE_PROJECT) และหมายเลข IP Address ของผู้ทำรายการ เพื่อความโปร่งใสและตรวจสอบได้ย้อนหลัง

---

## 4. ปัญหาที่พบบ่อยและแนวทางการแก้ไข (Troubleshooting)

### 4.1 ข้อมูลผู้ใช้น้ำในสัญญาใหม่/สัญญาปี 2568 ไม่แสดงรายละเอียด
* **อาการ:** ในหน้าจอตารางข้อมูลโครงการหรือตารางรายชื่อผู้ใช้น้ำ รายชื่อลูกค้าบางส่วนขึ้นคำว่า **"ไม่พบรายชื่อในฐานข้อมูล"** หรือ **"ไม่พบที่อยู่ในฐานข้อมูล"**
* **สาเหตุ:** ข้อมูลผู้ใช้ในโครงการดังกล่าวมีการบันทึกความเชื่อมโยงเลขสัญญาแล้วในตาราง `proj_cus` แต่โปรไฟล์ลูกค้าจริงยังไม่มีการดัมป์ (Dump) นำเข้าสู่ตารางโปรไฟล์ผู้ใช้ประปาหลัก (`customer`) จากระบบ PCIS
* **แนวทางแก้ไข:**
  1. ขอไฟล์ตารางประวัติผู้ใช้น้ำตัวล่าสุดจากเจ้าหน้าที่ไอทีของระบบ PCIS ของ กปภ.
  2. ดำเนินการนำเข้าไฟล์ SQL ดังกล่าวลงในตาราง `customer` เพื่อเติมพิกัดและรายละเอียดชื่อที่อยู่
  3. หลังจากอัปเดตข้อมูลฐานข้อมูลเสร็จสิ้น ให้ทำการรันการประมวลผลความสัมพันธ์ใหม่ โดยการเข้าไปยังโฟลเดอร์ `backend/` และพิมพ์คำสั่ง:
     ```bash
     node update_data.js
     ```
     *(คำสั่งนี้จะปรับปรุงพิกัดละติจูด/ลองจิจูดเฉลี่ยของทุกโครงการที่ได้รับผลกระทบ พร้อมปรับปรุงการคำนวณผู้ใช้งานสะสมจริงให้เป็นเวอร์ชันอัปเดตทันที)*

### 4.2 ปัญหาความไม่เข้ากันของชุดรหัสภาษาไทย (Collation Mismatch)
* **อาการ:** เมื่อเชื่อมโยงตารางข้อมูล PCIS ดิบ (จากฐานข้อมูลภายนอก) เข้ากับตารางระบบหลักแล้วเกิดข้อผิดพลาด SQL: `Illegal mix of collations for operation '='` บนเซิร์ฟเวอร์ MySQL
* **แนวทางแก้ไข:**
  - ทางทีมพัฒนาได้เตรียมสคริปต์สแกนปรับเปลี่ยนค่า Collation ของตารางข้อมูลดิบอย่างถาวรไว้แล้ว เพื่อไม่ให้กระทบต่อความเร็วคิวรี ให้รันคำสั่งแก้ไขผ่านเครื่องมือ Command Line:
    ```bash
    cd backend
    node fix_collation.js
    node fix_projects_collation.js
    ```
  - สคริปต์จะแปลงคอลัมน์สำคัญเช่น `cust_code`, `debt_ym` และ `project_code` ให้ใช้ชุดรหัสอักขระ `utf8mb4_unicode_ci` ซึ่งจะทำงานประสานงานกับตารางวิเคราะห์ของระบบ Dashboard ได้อย่างสมบูรณ์แบบโดยไม่ต้องใช้คำสั่งแปลงฟังก์ชันหน่วงเวลาระหว่างคิวรี

---
*จัดทำขึ้นโดยทีมพัฒนาระบบประเมินโครงการวางท่อขยายเขตจำหน่ายน้ำประปา กปภ.ข.6*
